import type { NextPage } from "next";
import { Input } from "@reactkads/styled-inputs";
import { Section } from "@reactkads/styled-landingpages";

const Home: NextPage = () => {
  return (
    <>
      <Section>
        <Input />
      </Section>
    </>
  );
};

export default Home;
